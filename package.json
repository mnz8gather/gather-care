{
  "name": "gather-care",
  "version": "0.0.1",
  "scripts": {
    "prepare": "husky",
    ":sub-modules": "pnpm -r exec rm -rf node_modules",
    ":sub-dist": "pnpm -r exec rm -rf dist",
    "clear": "sh ./zscript/clear.sh",
    "test-all": "pnpm -r test"
  },
  "packageManager": "pnpm@10.24.0",
  "devDependencies": {
    "husky": "^9.1.7",
    "lint-staged": "^16.2.7",
    "vitest": "^4.0.15"
  }
}